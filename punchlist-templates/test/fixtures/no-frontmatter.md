# Just a markdown file

No frontmatter here at all. The validator must reject this with a
missing-frontmatter error pointing at line 1.

## Output shape
Irrelevant — never reached.

## Golden exemplar
Irrelevant — never reached.
